language: c

compiler:
  - gcc-6

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - autoconf
    - automake
    - bison
    - gcc-6
    - gettext
    - help2man
    - texinfo

script: ./autogen.sh && ./configure && make && make check
